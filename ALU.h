#pragma once
#include <iostream>
#include <cstdlib>
#include <string>
#include <fstream>
#include <cstdint>
#include <inttypes.h>
#define MEMORY_SIZE 1024

using namespace std;

class ALU
{
private:
    /* data */
public:
    ALU(/* args */);
    ~ALU();
    /*
    adc
    add
    and (logical)
    cmp
    dec (decrement)
    div (unsigned)
    idiv (signed)
    imul (signed)
    inc (increment)
    mul
    neg (2's complement)
    not (1's complement)
    or (logical)
    rcl (rotate carry left)
    rcr (rotate carry right)
    rol (rotate left)
    ror (rotate right)
    sal (arithmetic left)
    sar (arithmetic right)
    shl (logical left)
    shr (logical right)
    sub
    */
};
